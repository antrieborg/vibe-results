# Vault Token Print Automation
===============

## Prompt
> ## Task
> write a script to print the value of VAULT_TOKEN
> ## Files
> - setup.sh
> ## Iteration History
> - Iteration 1 (1): success

## Overview
This automation task creates a script to print the value of the VAULT_TOKEN environment variable.

## Files
- `setup.sh`: A bash script that prints the value of the VAULT_TOKEN environment variable.

## Automation Description
The `setup.sh` script uses the `echo` command to print the value of the VAULT_TOKEN environment variable to the console.

## Iteration History
- Iteration 1 (1): success

## Installation and Usage
To use this automation task, simply run the `setup.sh` script from the terminal.

## Troubleshooting
There were no iteration failures to report.